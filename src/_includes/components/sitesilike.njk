<article class="center">
    <h2>Sites I Like!</h2>

    <section class="flexbuttons">
        {% for friend in friends %}
                <a href={{ friend.site }}>
                    <img src= {{ friend.button }}>
                </a>
        {% endfor %}
    </section>
</article>
